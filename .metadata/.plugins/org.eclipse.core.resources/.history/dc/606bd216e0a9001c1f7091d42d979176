package com.interfaces.abstraction;

public interface Bike {

	//Bike();// can't define constrauctors /// because all data members in interfaces are public static final by default, they are constants (
	void modelNumber();
	void Brand();
	void Safety();

	
	
}
